'use strict';

/**
 * business-directory router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::business-directory.business-directory');
