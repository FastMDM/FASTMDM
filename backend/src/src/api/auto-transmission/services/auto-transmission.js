'use strict';

/**
 * auto-transmission service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::auto-transmission.auto-transmission');
